{
  "name": "cistem",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "comment": "Root dev tooling only (one ESLint config + one Prettier config for both apps). App scripts run inside frontend/ and backend/. `npm run dev` starts Postgres + both apps.",
  "scripts": {
    "dev": "node scripts/dev.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.10.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.6",
    "globals": "^16.5.0",
    "prettier": "^3.9.6",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.70.0"
  }
}
